<nav class="navbar navbar-expand-lg navbar-light bg-info">
    <a class="navbar-brand text-light" href="/">АКОД</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            {% for item in menu %}
            {% if item.url == url.0 %}
            <li class="nav-item active">
                <a class="nav-link" href="/{{item.url}}">{{item.name}} <span class="sr-only">(current)</span></a>
            </li>
            {% else  %}
            <li class="nav-item">
                <a class="nav-link text-light" href="/{{item.url}}">{{item.name}} <span class="sr-only">(current)</span></a>
            </li>
            {% endif %}
            {% endfor %}
        </ul>
    </div>

</nav>